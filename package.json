{
	"name": "f-log",
	"version": "0.0.1",
	"description": "Library to simplify logging to any platform from your frontend",
	"main": "",
	"scripts": {
		"bundle-server-libs": "tsc --build tsconfig.server.json",
		"prepublish": "node scripts/pre-publish",
		"publish": "npm run bundle-server-libs && cd ./dist && npm run publish",
		"test": "vitest run"
	},
	"author": "Devesh Kumar",
	"license": "MIT",
	"dependencies": {
		"@google-cloud/logging": "^10.4.1"
	},
	"devDependencies": {
		"@types/express": "^4.17.17",
		"typescript": "^5.0.4",
		"vitest": "^0.31.0"
	}
}
